import React from 'react';
import {expect, jest, test} from '@jest/globals';
import Api from './Api';

/** Skeleton syntax, to be updated with actual tests*/

/**
test(input, () => {
  expect(method()).result('Hei');
});
*/
